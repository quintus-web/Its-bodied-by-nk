# Its-bodied-by-nk

Simple static site for NK Beauty — luxury wigs, makeup & bridal glam in Nairobi.

This repository currently contains a single `index.html` file.

Quick steps

- Open `index.html` in a browser to preview the site.
- To commit changes:
  - git add <files>
  - git commit -m "message"
  - git push

Notes

- The repository's remote `origin` already exists in your local Git config; if you need to change it run:

  git remote set-url origin https://github.com/quintus-web/Its-bodied-by-nk.git

- Created by project scaffolding helpers.